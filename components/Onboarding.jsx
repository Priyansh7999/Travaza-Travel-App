import React from 'react';
import { StyleSheet, View } from 'react-native';

const Onboarding = ({img, title, description, onClick}) => {
    return (
        <View>
            
        </View>
    );
}

const styles = StyleSheet.create({})

export default Onboarding;
